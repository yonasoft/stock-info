import React from "react";

type Props = {
  title: string;
  value: string;
};

function InfoCard({ title, value }: Props) {
  return <div className="size-6 rounded-lg"></div>;
}

export default InfoCard;
